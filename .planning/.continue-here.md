---
context: default
project: 6sigma_academy
paused_at: 2026-09-13T11:30:00+09:00
status: paused
---

# Continue Here — 6sigma_academy

## Current state

정적 웹앱(HTML/JS). 16강 교재 + 단원 퀴즈 + 종합 모의고사 + 통합 로그인 + 용어사전까지 구현된 상태. **GSD phase/roadmap 없음.** **git 저장소 없음.**

바로 직전 작업: 용어사전 인포그래픽 tip이 안 뜨던 버그 수정 완료 (`isInTipContext`로 `#glossary-detail-body` 포함).

## 핵심 파일

| 파일 | 역할 |
|------|------|
| `index.html` | UI/스타일/인증/뷰 전환/용어사전/팁 핸들러 |
| `courseData.js` | 16강 본문(인포그래픽 HTML) + 강당 퀴즈 10문항 |
| `tipCatalog.js` | 인포그래픽 호버·탭 상세 설명 |
| `glossaryData.js` | 용어사전 53항목 (가나다/ABC, detail HTML) |

## 이 세션에서 완료된 것

1. **통합 로그인** — 일반 회원가입 + 관리자(`admin`/`admin123`) 동일 모달. 세션·계정·로그인 기록·진행도 로컬 저장(계정별).
2. **방법론 보강** — 5강 선택 가이드, 13강 DFSS 변형(DMADV/IDOV/CDOV/DCCDI/DMADOV), 3·16강 린/애자일 연결.
3. **단원 말미** — 1~15강 정리+다음 안내, 16강 전과정 수료 인사 + 모의고사 안내 (`.lesson-outro`).
4. **용어사전** — 하단 탭「용어사전」, 검색/가나다·ABC, 상세+인포그래픽, 관련 용어·강의 링크.
5. **버그픽스** — 용어사전 tip 이벤트가 `#learn-content-area`에만 묶여 클릭이 무시되던 문제 → `isInTipContext()`로 glossary body 포함.

## 앱 구조 요약

- **뷰:** `view-landing` → `view-dashboard` / `view-map` / `view-glossary` / `view-mock` / `view-learn` / `view-quiz` / `view-result`
- **하단 탭:** 홈 · 커리큘럼 · 용어사전 · 모의고사
- **권한:** 관리자만 전 강/모의고사 미리보기; 일반은 순서 잠금 + 16강 이수 후 모의고사
- **퀴즈:** `quizMode` = `module` | `mock`, `activeQuizSet`
- **팁:** `enhanceInfographics()` + `#ig-popover` — 교재·용어사전 상세 모두 동작해야 함

## localStorage 키

- `sixsigma-auth-users-v1` — 계정
- `sixsigma-auth-session-v1` — 세션
- `sixsigma-auth-history-v1` — 로그인 기록
- `sixsigma-quiz-progress-v1:{userId}` — 이수·점수·모의고사

## 남은 일 / 미결정

- 명시적 미완료 기능 없음 (사용자 다음 요청 대기)
- git init/원격/커밋은 사용자 요청 시
- 수동 스모크: 로그인 → 용어사전 DMAIC 표 행 클릭 tip → 모의고사

## 재개 시 첫 행동

1. 이 파일 + `.planning/HANDOFF.json` 읽기
2. 사용자 최신 요청 확인
3. 필요 시 `index.html`의 `isInTipContext` / `openGlossaryTerm` / auth 블록부터 확인

## 주의

- 비밀번호는 로컬 데모용 **평문** 저장
- `courseData.js` / `glossaryData.js`는 용량 큼 — 부분 수정 시 StrReplace 또는 스크립트 생성 방식 선호
- graphify 미사용 (`graphify-out` 없음)
